-- ============================================================
-- 01_schema/01_dimensions.sql
-- PURPOSE : CREATE TABLE(s) — structure only, no data
-- DESIGN  : dim_company, dim_state, dim_product, dim_sub_product, dim_issue, dim_sub_issue — surrogate keys (D4), children UNIQUE (parent_id, name) (D6), '(not specified)' members (D9)
-- REF     : Context/schema_explanation.md
-- ============================================================

-- your query here
